import app from './app.js'; // import app


app.listen(3000,(err)=>{
    if(err){
        console.log(err);
    } else{// listen to port 3000
    console.log("Server is running on port 3000");
    }
});















